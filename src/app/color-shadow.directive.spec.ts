import { ColorShadowDirective } from './color-shadow.directive';

describe('ColorShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorShadowDirective();
    expect(directive).toBeTruthy();
  });
});

import { SearchConditionPipe } from './search-condition.pipe';

describe('SearchConditionPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchConditionPipe();
    expect(pipe).toBeTruthy();
  });
});

import { AdminUsersDirective } from './admin-users.directive';

describe('AdminUsersDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminUsersDirective();
    expect(directive).toBeTruthy();
  });
});
